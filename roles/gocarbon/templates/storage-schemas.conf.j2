{{ ansible_managed | comment }}

{% for schema in gocarbon_storage_schemas %}
[{{ schema.name }}]
pattern = {{ schema.pattern }}
retentions = {{ schema.retentions }}
compressed = {{ schema.compressed | default(false) }}

{% endfor %}